name: Generate Snake

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - name: Generate snake SVG
        uses: Platane/snk@v3
        with:
          github_user_name: YTFL
          github_token: ${{ secrets.GITHUB_TOKEN }}
          outputs: |
            dist/github-contribution-grid-snake.svg?color_snake=%1f6feb
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark&color_snake=%1f6feb

      - name: Prepare writable copies
        run: |
          mkdir -p dist-fixed
          cp dist/github-contribution-grid-snake.svg dist-fixed/
          cp dist/github-contribution-grid-snake-dark.svg dist-fixed/

      - name: Force refresh (avoid caching issues)
        run: |
          echo "<!-- $(date) -->" >> dist-fixed/github-contribution-grid-snake.svg
          echo "<!-- $(date) -->" >> dist-fixed/github-contribution-grid-snake-dark.svg

      - name: Deploy to snake-output branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: dist-fixed
          publish_branch: snake-output
          keep_files: false
